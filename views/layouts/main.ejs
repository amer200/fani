<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
         :root {
            /* ألوان باب الفهد المعتمدة */
            --primary: #1B3A57;
            /* الأزرق الداكن */
            --accent: #C5A059;
            /* الذهبي الملكي */
            --bg-body: #F4F7F9;
            /* خلفية هادئة */
            --sidebar-bg: #FFFFFF;
            /* سايد بار أبيض لفخامة أكتر */
            --text-main: #2C3E50;
            --sidebar-width: 260px;
        }
        
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--bg-body);
            color: var(--text-main);
            display: flex;
        }
        /* Sidebar Style المطور */
        
        .sidebar {
            width: var(--sidebar-width);
            height: 100vh;
            background-color: var(--sidebar-bg);
            position: fixed;
            border-left: 1px solid #e0e0e0;
            display: flex;
            flex-direction: column;
            padding: 0;
            box-shadow: 2px 0 10px rgba(0, 0, 0, 0.05);
            z-index: 1000;
        }
        
        .sidebar-header {
            text-align: center;
            padding: 30px 10px;
            background: #fff;
            border-bottom: 1px solid #f0f0f0;
        }
        /* ضبط مقاس الـ Logo */
        
        .sidebar-header img {
            max-width: 180px;
            /* الحجم المثالي للسايد بار */
            height: auto;
            display: block;
            margin: 0 auto 10px;
        }
        
        .sidebar-header small {
            display: block;
            color: var(--primary);
            font-weight: bold;
            letter-spacing: 1px;
            font-size: 12px;
            text-transform: uppercase;
        }
        
        .nav-links {
            list-style: none;
            padding: 20px 0;
            margin: 0;
        }
        
        .nav-links li {
            padding: 0;
            transition: 0.3s;
        }
        
        .nav-links a {
            padding: 15px 25px;
            color: var(--text-main);
            text-decoration: none;
            font-size: 15px;
            display: flex;
            align-items: center;
            transition: 0.3s;
            border-right: 4px solid transparent;
        }
        
        .nav-links li:hover a,
        .nav-links li.active a {
            background-color: #f8f9fa;
            color: var(--primary);
            border-right: 4px solid var(--accent);
        }
        
        .nav-links i {
            margin-left: 15px;
            color: var(--accent);
            font-size: 18px;
            width: 25px;
            text-align: center;
        }
        /* Main Content */
        
        .main-content {
            margin-right: var(--sidebar-width);
            padding: 40px;
            width: calc(100% - var(--sidebar-width));
            min-height: 100vh;
        }
        /* Responsive */
        
        @media (max-width: 768px) {
            .sidebar {
                width: 70px;
            }
            .sidebar-header small,
            .nav-links span {
                display: none;
            }
            .main-content {
                margin-right: 70px;
                width: calc(100% - 70px);
            }
            .sidebar-header img {
                max-width: 50px;
            }
        }
    </style>
</head>

<body>

    <aside class="sidebar">
        <div class="sidebar-header">
            <img src="/img/logo.png" alt="باب الفهد">
            <small>نظام إدارة الفنيين</small>
        </div>
        <ul class="nav-links">
            <li><a href="/admin/dashboard"><i class="fas fa-home"></i> <span>الرئيسية</span></a></li>
            <li><a href="/admin/add-tech"><i class="fas fa-user-plus"></i> <span>إضافة فني</span></a></li>
            <li><a href="/admin/add-task"><i class="fas fa-tasks"></i> <span>تكليف مهمة</span></a></li>
            <li><a href="/admin/tasks"><i class="fas fa-clipboard-list"></i> <span>جميع المهام</span></a></li>
            <li><a href="#"><i class="fas fa-archive"></i> <span>الأرشيف</span></a></li>
            <li style="margin-top: auto; border-top: 1px solid #f0f0f0;">
                <a href="#" style="color: #e74c3c;"><i class="fas fa-sign-out-alt" style="color: #e74c3c;"></i> <span>تسجيل الخروج</span></a>
            </li>
        </ul>
    </aside>

    <main class="main-content">
        <%- body %>
    </main>

</body>

</html>