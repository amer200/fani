<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | صرح فني</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;800&display=swap" rel="stylesheet">

    <style>
         :root {
            --bg-color: #0f0f0f;
            --card-bg: #1a1a1a;
            --accent: #00d1ff;
            /* اللون اللي مدي الروح للسيستم */
            --text-color: #e0e0e0;
        }
        
        body {
            background-color: var(--bg-color) !important;
            color: var(--text-color);
            font-family: 'Cairo', sans-serif;
            min-height: 100vh;
        }
        
        .edit-card {
            background: var(--card-bg);
            border-radius: 20px;
            border: 1px solid #333;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.5);
        }
        
        .form-label {
            color: #888;
            font-weight: 600;
            margin-bottom: 8px;
        }
        
        .custom-input {
            background: #252525 !important;
            border: 1px solid #444 !important;
            color: #fff !important;
            border-radius: 12px !important;
            padding: 12px 15px !important;
            transition: 0.3s ease;
        }
        
        .custom-input:focus {
            border-color: var(--accent) !important;
            box-shadow: 0 0 0 0.25 row rgba(0, 209, 255, 0.25) !important;
            outline: none;
        }
        
        .btn-save {
            background: var(--accent);
            color: #000;
            font-weight: 800;
            border: none;
            border-radius: 12px;
            padding: 12px 40px;
            transition: 0.3s;
        }
        
        .btn-save:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 209, 255, 0.4);
            opacity: 0.9;
        }
        
        .top-header {
            background: var(--card-bg);
            padding: 25px;
            border-radius: 15px;
            border-right: 5px solid var(--accent);
            margin-bottom: 30px;
        }
    </style>
</head>

<body>
    <div class="container py-5">
        <div class="top-header d-flex justify-content-between align-items-center shadow-sm">
            <h2 class="m-0" style="font-weight: 800;">
                <i class="fas fa-edit me-2" style="color: var(--accent);"></i>
                <%= title %>
            </h2>
            <a href="/admin/dashboard" class="text-muted text-decoration-none">
                <i class="fas fa-times-circle fs-4"></i>
            </a>
        </div>

        <div class="edit-card shadow-lg">
            <div class="card-body p-4 p-md-5">
                <form action="/admin/edit-task" method="POST">
                    <input type="hidden" name="id" value="<%= task._id %>">

                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label class="form-label">اسم العميل</label>
                            <input type="text" name="clientName" class="form-control custom-input" value="<%= task.clientName %>" required>
                        </div>
                        <div class="col-md-6 mb-4">
                            <label class="form-label">رقم هاتف العميل</label>
                            <input type="text" name="clientPhone" class="form-control custom-input" value="<%= task.clientPhone %>" required>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label class="form-label">عنوان العميل</label>
                        <input type="text" name="clientAddress" class="form-control custom-input" value="<%= task.clientAddress %>" required>
                    </div>

                    <div class="mb-4">
                        <label class="form-label">وصف العطل / المهمة</label>
                        <textarea name="description" class="form-control custom-input" rows="3" required><%= task.description %></textarea>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label class="form-label">توجيه لفني آخر</label>
                            <select name="assignedTechnician" class="form-select custom-input">
                                <% techs.forEach(tech => { %>
                                    <option value="<%= tech._id %>" 
                                        <%= task.assignedTechnician && task.assignedTechnician._id.toString() === tech._id.toString() ? 'selected' : '' %>>
                                        <%= tech.name %> - (<%= tech.specialty %>)
                                    </option>
                                <% }) %>
                            </select>
                        </div>
                        <div class="col-md-6 mb-4">
                            <label class="form-label">حالة المهمة</label>
                            <select name="status" class="form-select custom-input">
                                <option value="قيد الانتظار" <%= task.status === 'قيد الانتظار' ? 'selected' : '' %> style="color: #ffb800;">● قيد الانتظار</option>
                                <option value="تم الانجاز" <%= task.status === 'تم الانجاز' ? 'selected' : '' %> style="color: #00ff88;">● تم الانجاز</option>
                            </select>
                        </div>
                    </div>

                    <div class="mt-5 d-flex justify-content-between align-items-center">
                        <button type="submit" class="btn btn-save">
                            حفظ التعديلات ✅
                        </button>
                        <a href="/admin/dashboard" class="text-muted text-decoration-none" style="font-size: 14px;">
                            <i class="fas fa-chevron-left me-1"></i> إلغاء والعودة
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>